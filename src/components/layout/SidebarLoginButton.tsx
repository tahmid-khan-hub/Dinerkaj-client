import { Button } from "../ui/button";

export default function SidebarLoginButton() {
    return (
        <Button className="bg-(--accent-soft) text-(--accent) font-medium text-md py-5">Sign In</Button>
    )
}